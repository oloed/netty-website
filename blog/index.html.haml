---
title: Blog
layout: base
---

- for post in page.posts
  .post
    %h2
      %a{:href=>post.url}= post.title
    .author
      #{post.author} on #{post.date.strftime( '%d %B %Y' )}
    .body
      = post.content


= page.posts.links
.archive-links
  %a{ :href=>'archives.html' } Browse the archive

.notice{ :style=>'margin: 0 12px; ' }
  Wrote something related with Netty? <a href="mailto:trustin@gmail.com">Let us know!</a> We will let you publish your post here.

