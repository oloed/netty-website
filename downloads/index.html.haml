---
title: Downloads
layout: base
---

%h2 Downloads

%p Netty is dis&shy;trib&shy;uted un&shy;der <a href="http://www.apache.org/licenses/LICENSE-2.0">Apache Li&shy;cense, v2.0</a>. Please see the en&shy;closed <tt>NOTICE.txt</tt> for more in&shy;fo.

%ul
  - if ( site.unstableReleaseVersion )
    %li <a href="netty-#{site.unstableReleaseVersion}-dist.tar.bz2"><tt>netty-#{site.unstableReleaseVersion}-dist.tar.bz2</tt></a> (Unstable, #{site.unstableReleaseDate})
  - if ( site.stableReleaseVersion )
    %li <a href="netty-#{site.stableReleaseVersion}-dist.tar.bz2"><tt>netty-#{site.stableReleaseVersion}-dist.tar.bz2</tt></a> (Stable, #{site.stableReleaseDate})

%p <a href="https://github.com/netty/netty/issues/milestones?state=closed">Changelogs</a> and <a href="https://github.com/netty/netty/issues/milestones?state=open">road map</a> are available in our issue tracker.

%h3 Dependencies

%p Please note that Netty has <em>no mandatory external dependencies</em>. JDK 1.5 or above is all that you need to run Netty. However, the unstable releases might require a newer JDK version.

%h3 Getting notified on a new release

%p You can either subscribe to <a href="../blog/">our official blog</a> or follow <a href="https://twitter.com/netty_project">@netty_project</a> at Twitter.

%h3 Downloading from the Maven central repository

%p Add the following dependency section to your <tt>pom.xml</tt>:

%pre.brush:.xml
  :escaped
    <dependencies>
      ...
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty</artifactId>
        <version>X.Y.Z.Q</version>
        <scope>compile</scope>
      </dependency>
      ...
    </dependencies>

%h3 Previous releases

%p All previous releases can be downloaded from <a href="deprecated.html">here</a>.

%h3 Daily Snapshots

%p Daily snapshots can be found on the <a href="http://www.cloudbees.com">CloudBees</a> snapshot repository. Check it out <a href="http://repository-netty.forge.cloudbees.com/snapshot/">here</a>.
