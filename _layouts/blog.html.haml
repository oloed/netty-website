---
layout: base
---
.post
  %h2 #{page.title}
  .author
    #{page.author} on #{page.date.strftime( '%d %B %Y' )}
  .body
    ~ content
  .comments
    = page.intense_debate_comments

.post-navigation
  .previous
    - if page.previous_post
      Previous:
      %a{:href => page.previous_post.url}= page.previous_post.title
  .next
    - if page.next_post
      Next:
      %a{:href => page.next_post.url}= page.next_post.title

